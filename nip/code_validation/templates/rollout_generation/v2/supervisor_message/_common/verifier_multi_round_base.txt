{%- if num_questions_left == 0 -%}
    You have no more questions remaining, and must now make a decision. {{ verifier_decision_instructions | capitalise_first_letter }}
{%- else -%}
    {%- block remaining_questions -%}
        You have {{ num_questions_left }} {{ "question" | add_s_plural(num_questions_left) }} remaining.
    {%- endblock remaining_questions -%}
    {{ " " }}
    {%- block either_ask -%}
        Either ask a question to the prover, or make a decision.
    {%- endblock either_ask -%}
    {{ " " }}
    {%- block question_instructions -%}
        To ask a question, write "Question for Expert: [question]".
    {%- endblock question_instructions -%}
    {{ " " }}To make a decision {{ verifier_decision_instructions }}
{%- endif -%}