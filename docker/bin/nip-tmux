#!/bin/bash

# This script is used to start a tmux session for the nip project

# Get the directory of the script
script_dir=`dirname -- "$( readlink -f -- "$0"; )";`

# Run the python script with all the arguments
(cd /root/neural-interactive-proofs && uv run python "$script_dir/nip-tmux.py" $@)